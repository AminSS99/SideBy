🎯 **What:**
Extracted code from `frontend/src/pages/Index.tsx` into five distinct components (`Header`, `Hero`, `Features`, `Orchestration`, and `Footer`).

💡 **Why:**
The original `Index.tsx` was a monolith file spanning over 500 lines. Breaking these components down drastically improves readability and maintainability.

✅ **Verification:**
Verified code syntax and validity by running the TypeScript compiler (`pnpm exec tsc --noEmit`) and ESLint (`pnpm lint`). Both passed successfully. Visual changes are expected to be exactly the same as previously, since the structure logic was kept identical.

✨ **Result:**
`frontend/src/pages/Index.tsx` was reduced to roughly 250 lines and logic was effectively encapsulated in `/frontend/src/pages/index/components/`.
