import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.elegantpuffinsoar',
  appName: 'elegant-puffin-soar',
  webDir: 'dist'
};

export default config;
